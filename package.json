{
  "name": "itikaf",
  "version": "0.1.0",
  "private": true,
  "description": "Itikaf — audio-journal a dilemma, get a matched dua/verse and a sunnah istighfar.",
  "scripts": {
    "build:vendor": "esbuild scripts/purchases-entry.js --bundle --format=iife --outfile=vendor/purchases-bundle.js",
    "sync": "npm run build:vendor && mkdir -p www/vendor && cp index.html sw.js manifest.json privacy.html www/ && cp vendor/purchases-bundle.js www/vendor/ && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@revenuecat/purchases-capacitor": "^9.2.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "esbuild": "^0.28.1"
  }
}
